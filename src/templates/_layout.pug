doctype html

block locals
    - const title = 'Pass Generator';

html(lang="ja")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        title #{title}
    body
        block content
